apiVersion: v1
kind: Service
metadata:
  name: earthshaker-pihole
  namespace: shawshank
spec:
  selector:
    app: earthshaker-pihole
  type: NodePort
  ports: 
  - name: http
    protocol: TCP
    port: 80
    targetPort: 80
  - name: dns-tcp
    protocol: TCP
    port: 53
    targetPort: 53
  - name: dns-udp
    protocol: UDP
    port: 53
    targetPort: 53
  externalIPs:
  - 10.0.0.20 # Node IP Address
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: earthshaker-pihole
  namespace: shawshank
  labels:
    app: earthshaker-pihole
spec:
  replicas: 1
  selector:
    matchLabels:
      app: earthshaker-pihole
  template:
    metadata:
      labels:
        app: earthshaker-pihole
    spec:
      hostname: earthshaker-pihole
      nodeSelector:
        kubernetes.io/hostname: earthshaker.shawshank # Only deploy to Zeus Node    
      containers:
      - name: earthshaker-pihole
        image: diginc/pi-hole:debian_v3.3.1
        imagePullPolicy: IfNotPresent
        env:
        - name: TZ
          value: "America/New_York"
        - name: DNS1
          value: 8.8.8.8
        - name: DNS2
          value: 8.8.4.4
        - name: ServerIP
          value: 10.0.0.20 # Node IP Address
        ports:
        - containerPort: 80
          protocol: TCP
        - containerPort: 53
          protocol: TCP
        - containerPort: 53
          protocol: UDP
        resources:
          requests:
            memory: "256Mi"     # 256mb
            cpu: "200m"         # 1/5 CPU
          limits:               
            memory: "384Mi"     # 384mb
            cpu: "444m"         # 2.25/5 CPU
