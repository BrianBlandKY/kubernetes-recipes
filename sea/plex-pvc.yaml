apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: nfs-plex
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 6Ti
  storageClassName: plex